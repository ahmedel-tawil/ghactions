on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: SET API KEY
        run: echo "API_KEY=11234455353" >> $GITHUB_ENV

      - name: use API key
        run: echo " using api key $API_KEY"